COMMENT ON VIEW "util"."worker_domain_traceability_violations" IS 'Service-role audit view for post-cutover worker-produced domain rows that are not traceable to worker_jobs. Historical pre-cutover nullable rows are intentionally ignored.';
