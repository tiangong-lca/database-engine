CREATE UNIQUE INDEX "lca_package_import_prepare_idempotency_uk" ON "private"."lca_package_artifacts" USING "btree" ((("metadata" ->> 'requested_by'::"text")), (("metadata" ->> 'import_prepare_idempotency_key'::"text"))) WHERE (("artifact_kind" = 'import_source'::"text") AND ("metadata" ? 'import_prepare_idempotency_key'::"text") AND (NULLIF(("metadata" ->> 'import_prepare_idempotency_key'::"text"), ''::"text") IS NOT NULL));
