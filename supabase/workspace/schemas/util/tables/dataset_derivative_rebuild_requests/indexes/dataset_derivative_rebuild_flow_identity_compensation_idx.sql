CREATE INDEX "dataset_derivative_rebuild_flow_identity_compensation_idx" ON "util"."dataset_derivative_rebuild_requests" USING "btree" ("actor_user_id", "target_table", "target_id", "target_version", "admitted_at" DESC, "id" DESC) INCLUDE ("batch_id", "plan_sha256", "operation_id", "action_id", "reason_code", "expected_json_ordered_sha256", "status", "action_audit_id", "summary_audit_id") WHERE ("batch_id" IS NULL);
